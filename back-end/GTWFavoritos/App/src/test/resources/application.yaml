spring:
  datasource:
    url: jdbc:mysql://localhost:3307/favoritos_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Madrid
    username: admin
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    enabled: false
    registerWithEureka: false
    fetchRegistry: false

patrimonio:
  service:
    url: http://localhost:8080
